version: '3.8'

services:
  web-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: modularai-web-app
    ports:
      - "3010:3000"
    environment:
      - NODE_ENV=production
